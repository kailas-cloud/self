apiVersion: v1
kind: Service
metadata:
  name: claude-router
  namespace: llm
spec:
  selector:
    app: claude-router
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3456
  type: ClusterIP
